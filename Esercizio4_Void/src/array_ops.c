#include "array_ops.h"
#include <stdint.h>

void applica_a_tutti(void *array, size_t elementi, size_t size_elemento, 
                     void (*operazione)(void*)) {
    /* TODO: scorrere l'array e applicare operazione a ogni elemento */
    /* TODO: gestire array == NULL o operazione == NULL */
}
